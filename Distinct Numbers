#include <bits/stdc++.h>
using namespace std;
#define mod 1000000007;

int main()
{
    long n;
    cin>>n;
    set<long long>s;
    for(int i=0;i<n;i++)
    {
        long long x;
        cin>>x;
        s.insert(x);
    }
    cout<<s.size();
}
